  <div class="container">
    <div class="row">
        <tbody>
          <tr>
            <td><%= image_tag(line_item.product.product_image_url) %></td>
            <td><%= line_item.product.product_name %></td>
            <td><%= truncate(line_item.product.product_details, length: 140) %></td>
            <td><%= number_to_currency(line_item.product.sales_price, :unit => "RWF") %></td>
            <%= form_with(model: line_item, local: true) do |form| %>
            <td><%= form.number_field :quantity, size:"3" %></td>
            <td><%= number_to_currency(line_item.total_price, :unit => "RWF") %></td>
            <td><%= form.submit class:"btn btn-primary" %></td>
            <% end %>
            <td>
            <%= link_to 'Remove Item', line_item, method: :delete, data: { confirm: "Are you sure? " }, class: "btn btn-danger" %>
            </td>
          </tr>
        </tbody>
    </div>
  </div>